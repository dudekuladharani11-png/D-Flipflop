# D-Flipflop
A **D Flip-Flop** is a sequential logic circuit that stores one bit of data. On the active clock edge, the output follows the D input and remains unchanged until the next clock edge. It is widely used in registers, memory, counters, and digital systems.
